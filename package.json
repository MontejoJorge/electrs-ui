{
   "name": "electrs-ui",
   "version": "0.9",
   "private": true,
   "description": "",
   "author": "",
   "scripts": {
     "build:frontend": "pnpm --filter frontend run build",
     "dev:backend": "pnpm --filter backend run start",
     "dev:frontend": "pnpm --filter frontend run serve",
     "test": "echo \"Error: no test specified\" && exit 1"
   },
   "main": "index.js",
   "dependencies": {
     "autoprefixer": "^10",
     "postcss": "^8",
     "tailwindcss": "^3"
   },
   "devDependencies": {
     "vue-cli-plugin-tailwind": "~3.0.0"
   },
   "workspaces": [
     "apps/*"
   ]
 }
 